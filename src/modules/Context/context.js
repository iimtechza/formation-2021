import React from "react";

const _App = React.createContext(/* some value */);

const APP = React.createContext(/* some value */);

const app = React.createContext(/* some value */);

const { Provider, Consumer } = APP;

export default APP;
